> Then login jump server through database server
     $ ssh -i key-name ec2-user@PrivateIPofdatabaseserver
     $ sudo yum install mariadb105* -y
     $ sudo systemctl start mariadb.service
     $ sudo systemctl enable mariadb.service
     $ Then get rds database & copy bundle link then paste in CLI
     $ Then go to RDS database & copy database endpoint then paste CLI
     $ mysql -h <endpointofDatabase> -u <username> -p<password>
     $ create database studentapp;
     $ use studentapp;
     $ CREATE TABLE if not exists students(student_id INT NOT NULL AUTO_INCREMENT,
       student_name VARCHAR(100) NOT NULL,
       student_addr VARCHAR(100) NOT NULL,
       student_age VARCHAR(3) NOT NULL,
       student_qual VARCHAR(20) NOT NULL,
       student_percent VARCHAR(10) NOT NULL,
       student_year_passed VARCHAR(10) NOT NULL,
       PRIMARY KEY (student_id)
       );
     $ show tables;
     $ select * from students;
     $ exit

